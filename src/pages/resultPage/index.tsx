import styled from 'styled-components';

export default function ResultPage() {
    return (
        <Container>
            <header>Play Result</header>
        </Container>
    );
}

const Container = styled.div`
    box-sizing: border-box;
    height: 100vh;
    background-color: #000;
    color: #fff;
`;
